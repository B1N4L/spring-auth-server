spring:
  application:
    name: auth-server
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/microservices
    username: root
    password: root

server:
  port: 8085
#  servlet:
#    context-path: /auth-api
